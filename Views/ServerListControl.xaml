<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="App2.Views.ServerListControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:models="using:App2.Models"
             xmlns:viewmodels="using:App2.ViewModels"
             mc:Ignorable="d"
             d:DesignHeight="500"
             d:DesignWidth="420">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0"
                    Orientation="Vertical">
            <TextBlock Text="服务器列表"
                       FontSize="20"
                       FontWeight="SemiBold" />
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Spacing="8"
                        Margin="0,8,0,0">
                <DropDownButton x:Name="BtnAdd"
                                Content="添加"
                                Padding="12,6">
                    <DropDownButton.Flyout>
                        <MenuFlyout Placement="BottomEdgeAlignedLeft">
                            <MenuFlyoutItem Text="从SS链接导入"
                                            Command="{x:Bind ViewModel.ImportFromLinkCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE77F;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                            <MenuFlyoutItem Text="手动添加新服务器"
                                            Command="{x:Bind ViewModel.AddManualCommand}"
                                            FontWeight="Normal">
                                <MenuFlyoutItem.Icon>
                                    <FontIcon Glyph="&#xE710;" />
                                </MenuFlyoutItem.Icon>
                            </MenuFlyoutItem>
                        </MenuFlyout>
                    </DropDownButton.Flyout>
                </DropDownButton>
                <Button x:Name="BtnEdit"
                        Command="{x:Bind ViewModel.EditServerCommand}"
                        IsEnabled="{x:Bind ViewModel.CanEditServer, Mode=OneWay}"
                        Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE70F;"
                                  Margin="0,0,5,0" />
                        <TextBlock Text="编辑" />
                    </StackPanel>
                </Button>

                <Button x:Name="BtnRemove"
                        Command="{x:Bind ViewModel.RemoveServerCommand}"
                        IsEnabled="{x:Bind ViewModel.CanRemoveServer, Mode=OneWay}"
                        Padding="12,6">
                    <StackPanel Orientation="Horizontal">
                        <FontIcon Glyph="&#xE74D;"
                                  Margin="0,0,5,0" />
                        <TextBlock Text="删除" />
                    </StackPanel>
                </Button>
            </StackPanel>
        </StackPanel>

        <AutoSuggestBox x:Name="ServerSearchBox"
                        Grid.Row="1"
                        Margin="0,16,0,0"
                        PlaceholderText="搜索服务器"
                        HorizontalAlignment="Stretch"
                        VerticalAlignment="Center"
                        UpdateTextOnSelect="True"
                        TextChanged="ServerSearchBox_TextChanged"
                        SuggestionChosen="ServerSearchBox_SuggestionChosen"
                        QuerySubmitted="ServerSearchBox_QuerySubmitted"
                        TextMemberPath="Name">
            <AutoSuggestBox.ItemTemplate>
                <DataTemplate x:DataType="models:ServerEntry">
                    <TextBlock Text="{x:Bind Name, Mode=OneWay}" />
                </DataTemplate>
            </AutoSuggestBox.ItemTemplate>
        </AutoSuggestBox>

        <ListView Grid.Row="2"
                  x:Name="ServersListView"
                  Margin="0,16,0,0"
                  ItemsSource="{x:Bind ViewModel.Servers, Mode=OneWay}"
                  SelectedItem="{x:Bind ViewModel.SelectedServer, Mode=TwoWay}"
                  CanDragItems="True"
                  CanReorderItems="True"
                  AllowDrop="True"
                  SelectionMode="Single"
                  ScrollViewer.VerticalScrollBarVisibility="Auto">

            <ListView.ItemTemplate>
                <DataTemplate x:DataType="models:ServerEntry">
                    <StackPanel Orientation="Vertical"
                                Spacing="6"
                                Margin="0,12,0,12">
                        <Grid ColumnDefinitions="*, Auto"
                              VerticalAlignment="Center">

                            <TextBlock Text="{x:Bind Name, Mode=OneWay}"
                                       FontSize="16"
                                       FontWeight="SemiBold"
                                       Grid.Column="0"
                                       TextTrimming="CharacterEllipsis" />

                            <Border Background="{ThemeResource SystemFillColorSuccessBrush}"
                                    CornerRadius="4"
                                    Padding="6,2"
                                    Visibility="{x:Bind IsActive, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                                    Grid.Column="1"
                                    Margin="8,0,0,0">
                                <TextBlock Text="● Activated"
                                           FontSize="12"
                                           FontWeight="SemiBold"
                                           Foreground="White" />
                            </Border>
                        </Grid>

                        <StackPanel Orientation="Horizontal"
                                    Spacing="8">
                            <TextBlock Text="{x:Bind Host, Mode=OneWay}"
                                       FontSize="13"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock Text="{x:Bind Port, Mode=OneWay}"
                                       FontSize="13"
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <Border Background="{ThemeResource AccentFillColorDefaultBrush}"
                                    CornerRadius="4"
                                    Padding="6,2"
                                    Margin="4,0,0,0">
                                <TextBlock Text="{x:Bind Method, Mode=OneWay}"
                                           FontSize="11"
                                           Foreground="{ThemeResource TextOnAccentFillColorPrimaryBrush}" />
                            </Border>
                        </StackPanel>
                    </StackPanel>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</UserControl>
