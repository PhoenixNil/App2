<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="App2.Views.ControlPanelControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:viewmodels="using:App2.ViewModels"
             mc:Ignorable="d">

    <Border Margin="0,10,0,0"
            Padding="8,12">
        <Grid RowSpacing="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0"
                        Orientation="Horizontal"
                        VerticalAlignment="Center"
                        Spacing="12">
                <ToggleButton MinWidth="96"
                              Content="{x:Bind ViewModel.StartStopButtonContent, Mode=OneWay}"
                              IsChecked="{x:Bind ViewModel.StartStopButtonChecked, Mode=OneWay}"
                              Command="{x:Bind ViewModel.StartStopCommand}" />
                <StackPanel Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Spacing="8">
                    <FontIcon Glyph="&#xECCC;"
                              Foreground="{x:Bind ViewModel.StatusIconColor, Mode=OneWay, Converter={StaticResource ColorToBrushConverter}}" />
                    <TextBlock x:Name="TxtStatus"
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Text="{x:Bind ViewModel.StatusText, Mode=OneWay}" />
                </StackPanel>

                <Border Padding="8,4"
                        Background="{x:Bind ViewModel.RouteModeBadgeBackgroundColor, Mode=OneWay, Converter={StaticResource ColorToBrushConverter}}"
                        Visibility="{x:Bind ViewModel.IsRouteModeBadgeVisible, Mode=OneWay, Converter={StaticResource BooleanToVisibilityConverter}}"
                        VerticalAlignment="Center">
                    <StackPanel Orientation="Horizontal"
                                Spacing="4">
                        <FontIcon Glyph="{x:Bind ViewModel.RouteModeBadgeIcon, Mode=OneWay}"
                                  FontSize="12"
                                  Foreground="{x:Bind ViewModel.RouteModeBadgeForegroundColor, Mode=OneWay, Converter={StaticResource ColorToBrushConverter}}"
                                  VerticalAlignment="Center" />
                        <TextBlock Text="{x:Bind ViewModel.RouteModeBadgeText, Mode=OneWay}"
                                   FontSize="12"
                                   FontWeight="SemiBold"
                                   Foreground="{x:Bind ViewModel.RouteModeBadgeForegroundColor, Mode=OneWay, Converter={StaticResource ColorToBrushConverter}}"
                                   VerticalAlignment="Center" />
                    </StackPanel>
                </Border>

                <Button Background="Transparent"
                        BorderBrush="Transparent"
                        VerticalAlignment="Center">
                    <Button.Content>
                        <FontIcon Glyph="&#xE713;"
                                  FontSize="13" />
                    </Button.Content>
                    <Button.Flyout>
                        <MenuFlyout>
                            <MenuFlyoutItem Text="编辑本地端口"
                                            Command="{x:Bind ViewModel.EditLocalPortCommand}" />
                            <MenuFlyoutItem Text="查看日志"
                                            Command="{x:Bind ViewModel.ViewLogsCommand}" />
                            <MenuFlyoutItem Text="设置主题"
                                            Command="{x:Bind ViewModel.ToggleThemePickerCommand}" />
                            <ToggleMenuFlyoutItem Text="开机启动"
                                                  IsChecked="{x:Bind ViewModel.IsAutoStartEnabled, Mode=TwoWay}"/>
                            <MenuFlyoutSubItem Text="路由设置"
                                               IsEnabled="{x:Bind ViewModel.CanEditRouteSettings, Mode=OneWay}">
                                <MenuFlyoutItem Text="全局路由"
                                                Command="{x:Bind ViewModel.ChangeRouteModeCommand}"
                                                CommandParameter="{x:Bind ViewModel.FalseValue, Mode=OneWay}" />
                                <MenuFlyoutItem Text="绕过大陆"
                                                Command="{x:Bind ViewModel.ChangeRouteModeCommand}"
                                                CommandParameter="{x:Bind ViewModel.TrueValue, Mode=OneWay}" />
                            </MenuFlyoutSubItem>
                        </MenuFlyout>
                    </Button.Flyout>
                </Button>
            </StackPanel>

            <TeachingTip Title="色彩模式"
                         PreferredPlacement="Auto"
                         IsOpen="{x:Bind ViewModel.IsThemePickerOpen, Mode=TwoWay}"
                         PlacementMargin="20">
                <TeachingTip.Content>
                    <StackPanel Orientation="Horizontal"
                                Spacing="10">
                        <Button MinWidth="70"
                                Padding="12,8"
                                Command="{x:Bind ViewModel.ChangeThemeCommand}"
                                CommandParameter="{x:Bind ViewModel.LightTheme, Mode=OneWay}"
                                IsEnabled="{x:Bind ViewModel.IsLightThemeEnabled, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="6">
                                <FontIcon Glyph="&#xE706;"
                                          FontSize="14" />
                                <TextBlock Text="浅色"
                                           FontSize="14" />
                            </StackPanel>
                        </Button>
                        <Button MinWidth="70"
                                Padding="12,8"
                                Command="{x:Bind ViewModel.ChangeThemeCommand}"
                                CommandParameter="{x:Bind ViewModel.DarkTheme, Mode=OneWay}"
                                IsEnabled="{x:Bind ViewModel.IsDarkThemeEnabled, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="6">
                                <FontIcon Glyph="&#xE708;"
                                          FontSize="14" />
                                <TextBlock Text="深色"
                                           FontSize="14" />
                            </StackPanel>
                        </Button>
                        <Button MinWidth="70"
                                Padding="12,8"
                                Command="{x:Bind ViewModel.ChangeThemeCommand}"
                                CommandParameter="{x:Bind ViewModel.DefaultTheme, Mode=OneWay}"
                                IsEnabled="{x:Bind ViewModel.IsDefaultThemeEnabled, Mode=OneWay}">
                            <StackPanel Orientation="Horizontal"
                                        Spacing="6">
                                <FontIcon Glyph="&#xE770;"
                                          FontSize="14" />
                                <TextBlock Text="跟随系统"
                                           FontSize="14" />
                            </StackPanel>
                        </Button>
                    </StackPanel>
                </TeachingTip.Content>
            </TeachingTip>

            <Grid Grid.Row="1"
                  ColumnSpacing="24">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="0"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Spacing="16">
                    <Border Padding="12,8">
                        <StackPanel Orientation="Horizontal"
                                    Spacing="2">
                            <TextBlock Text="本地端口："
                                       Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                            <TextBlock x:Name="TxtLocalPortValue"
                                       Text="{x:Bind ViewModel.LocalPortText, Mode=OneWay}"
                                       FontWeight="SemiBold" />
                        </StackPanel>
                    </Border>
                </StackPanel>

                <StackPanel Grid.Column="1"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            VerticalAlignment="Center"
                            Spacing="8">
                    <TextBlock Text="代理模式: "
                               VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Margin="0,0,4,0" />
                    <ComboBox SelectedIndex="{x:Bind ViewModel.ProxyModeIndex, Mode=TwoWay}"
                              IsEnabled="{x:Bind ViewModel.CanChangeProxyMode, Mode=OneWay}"
                              MinWidth="100">
                        <x:String>全局代理</x:String>
                        <x:String>PAC模式</x:String>
                        <x:String>直连模式</x:String>
                    </ComboBox>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</UserControl>
