<?xml version="1.0" encoding="utf-8"?>
<UserControl x:Class="App2.Views.ServerDetailControl"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:controls="using:App2.Controls"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d"
             d:DesignWidth="500"
             d:DesignHeight="600">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0"
                   Text="&#x670D;&#x52A1;&#x5668;&#x8BE6;&#x60C5;"
                   FontSize="20"
                   FontWeight="SemiBold"
                   Margin="0,0,0,16" />

        <ScrollViewer Grid.Row="1"
                      VerticalScrollBarVisibility="Auto"
                      HorizontalScrollBarVisibility="Disabled"
                      Margin="0,0,0,12">
            <StackPanel Orientation="Vertical"
                        Spacing="8"
                        Padding="0,0,12,80">

                <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="16"
                        Padding="20">
                    <StackPanel Orientation="Vertical"
                                Spacing="12">

                        <Grid>
                            <Grid x:Name="InfoGrid"
                                  ColumnSpacing="12"
                                  RowSpacing="10">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto" />
                                    <ColumnDefinition Width="*" />
                                </Grid.ColumnDefinitions>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                    <RowDefinition Height="Auto" />
                                </Grid.RowDefinitions>

                                <StackPanel Orientation="Horizontal"
                                            Grid.Row="0"
                                            Grid.Column="0"
                                            VerticalAlignment="Center"
                                            Spacing="8">
                                    <SymbolIcon Symbol="Contact" />
                                    <TextBlock Text="&#x540D;&#x79F0;"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                                <TextBlock Grid.Row="0"
                                           Grid.Column="1"
                                           Text="{x:Bind ViewModel.SelectedName, Mode=OneWay}"
                                           FontSize="16"
                                           FontWeight="SemiBold"
                                           TextWrapping="WrapWholeWords"
                                           VerticalAlignment="Center"
                                           Margin="0,0,32,0" />

                                <StackPanel Orientation="Horizontal"
                                            Grid.Row="1"
                                            Grid.Column="0"
                                            Spacing="8">
                                    <FontIcon Foreground="{ThemeResource AccentAcrylicBackgroundFillColorDefaultBrush}"
                                              Glyph="&#xE774;" />
                                    <TextBlock Text="&#x5730;&#x5740;"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                                <TextBlock Grid.Row="1"
                                           Grid.Column="1"
                                           Text="{x:Bind ViewModel.SelectedHost, Mode=OneWay}"
                                           FontSize="16"
                                           TextWrapping="WrapWholeWords" />

                                <StackPanel Orientation="Horizontal"
                                            Grid.Row="2"
                                            Grid.Column="0"
                                            Spacing="8">
                                    <FontIcon Foreground="#73d3ff" Glyph="&#xE95F;" />
                                    <TextBlock Text="&#x7AEF;&#x53E3;"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                                <TextBlock Grid.Row="2"
                                           Grid.Column="1"
                                           Text="{x:Bind ViewModel.SelectedPort, Mode=OneWay}"
                                           FontSize="16" />

                                <StackPanel Orientation="Horizontal"
                                            Grid.Row="3"
                                            Grid.Column="0"
                                            Spacing="8">
                                    <FontIcon Foreground="#ffb347" Glyph="&#xE8D7;" />
                                    <TextBlock Text="&#x52A0;&#x5BC6;"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}" />
                                </StackPanel>
                                <TextBlock Grid.Row="3"
                                           Grid.Column="1"
                                           Text="{x:Bind ViewModel.SelectedMethod, Mode=OneWay}"
                                           FontSize="16" />
                            </Grid>

                            <controls:CopyButton HorizontalAlignment="Right"
                                                 VerticalAlignment="Top"
                                                 Padding="7,6,7,6"
                                                 MinWidth="32"
                                                 MinHeight="32"
                                                 Content="&#xE8C8;"
                                                 TextToCopy="{x:Bind ViewModel.SelectedSsLink, Mode=OneWay}"
                                                 CopiedMessage="SS &#x94FE;&#x63A5;&#x5DF2;&#x590D;&#x5236;"
                                                 Click="ShareCopyButton_Click"
                                                 AutomationProperties.Name="&#x590D;&#x5236; SS &#x94FE;&#x63A5;"
                                                 ToolTipService.ToolTip="&#x590D;&#x5236; SS &#x94FE;&#x63A5;" />
                        </Grid>

                        <Border Background="{ThemeResource LayerFillColorDefaultBrush}"
                                BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}"
                                BorderThickness="1"
                                CornerRadius="12"
                                Padding="16">
                            <Grid ColumnDefinitions="*,Auto,Auto"
                                  ColumnSpacing="12"
                                  VerticalAlignment="Center">
                                <StackPanel Orientation="Horizontal"
                                            Spacing="6">
                                    <FontIcon Glyph="&#xE9D9;"
                                              Foreground="#A67CA8"
                                              VerticalAlignment="Center" />
                                    <TextBlock Text="&#x7F51;&#x7EDC;&#x5EF6;&#x8FDF;"
                                               FontSize="13"
                                               Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                               VerticalAlignment="Center" />
                                </StackPanel>

                                <TextBlock Grid.Column="1"
                                           Text="{x:Bind ViewModel.LatencyText, Mode=OneWay}"
                                           Foreground="{x:Bind ViewModel.LatencyColor, Mode=OneWay, Converter={StaticResource ColorToBrushConverter}}"
                                           FontSize="20"
                                           FontWeight="SemiBold"
                                           VerticalAlignment="Center"
                                           Margin="0,0,8,0" />

                                <Button Grid.Column="2"
                                        Command="{x:Bind ViewModel.TestLatencyCommand}"
                                        Background="Transparent"
                                        BorderBrush="Transparent"
                                        Padding="8"
                                        ToolTipService.ToolTip="&#x91CD;&#x65B0;&#x6D4B;&#x8BD5;&#x5EF6;&#x8FDF;"
                                        VerticalAlignment="Center">
                                    <FontIcon Glyph="&#xE72C;"
                                              FontSize="14" />
                                </Button>
                            </Grid>
                        </Border>

                    </StackPanel>
                </Border>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</UserControl>
